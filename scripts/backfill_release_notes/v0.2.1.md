## box-open-sdk 0.2.1

Patch release — no API surface changed. Ships work that accumulated since
0.2.0 and couldn't go out under it, since a published version is immutable:
a LICENSE file in every SDK (each manifest already declared MIT while no
repo carried the text — pkg.go.dev was reading the Go module as "License:
None detected"), a doc-only root package so the Go module has documentation
on pkg.go.dev instead of a bare directory listing, README prose in the
project's voice, the Maven Central and javadoc.io badges Java was missing,
`autoPublish`, and refreshed package-manifest descriptions.

Changes since v0.2.0:

- ship: auto-publish to Central, and refresh the SDK descriptions ([#93](https://github.com/unofficialbox/box-gantry/pull/93))
- ship: release the fleet from a guarded script and workflow (`scripts/release.sh`) ([#94](https://github.com/unofficialbox/box-gantry/pull/94))
- ship: ship a LICENSE, fix the Go module page, and refresh the README voice ([#95](https://github.com/unofficialbox/box-gantry/pull/95))

Full diff: https://github.com/unofficialbox/box-gantry/compare/cf200e8...fa14253

> Not affiliated with, authorized, or endorsed by Box, Inc.
> A community, generated client.
