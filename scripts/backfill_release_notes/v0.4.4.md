## box-open-sdk 0.4.4

Patch release: integrates the Apex unlocked package build into the fleet
release — an Apex release now links the installable `04t…` package version
and its one-click install URL — and fixes an Apex compile error the
multipart fix (0.4.3) introduced (`Blob` has no `+` operator in Apex; the
multipart body is now assembled via a hex round-trip instead).

Changes since v0.4.3:

- ci: integrate the Apex unlocked package build into the fleet release ([#133](https://github.com/unofficialbox/box-gantry/pull/133))
- fix(apex): `Blob` has no `+` operator — build the multipart body via hex, not concatenation ([#134](https://github.com/unofficialbox/box-gantry/pull/134))

Full diff: https://github.com/unofficialbox/box-gantry/compare/fff97b5...83833ef

> Not affiliated with, authorized, or endorsed by Box, Inc.
> A community, generated client.
