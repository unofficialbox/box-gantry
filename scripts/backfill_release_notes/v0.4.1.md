## box-open-sdk 0.4.1

Patch release: splits the generated `schemas.{go,rs,ts}` monoliths into one
file per manager — a purely additive, non-breaking file-layout change with
zero public API impact, built on a new per-manager schema reachability
index in the semantic-analysis layer.

Changes since v0.4.0:

- sema: add a per-manager schema reachability index (`decl_managers`) ([#119](https://github.com/unofficialbox/box-gantry/pull/119))
- Land the Rust and TypeScript schema-file splits (Go's landed earlier in the same cycle) ([#123](https://github.com/unofficialbox/box-gantry/pull/123))

Full diff: https://github.com/unofficialbox/box-gantry/compare/14fbc3c...a09465f

> Not affiliated with, authorized, or endorsed by Box, Inc.
> A community, generated client.
